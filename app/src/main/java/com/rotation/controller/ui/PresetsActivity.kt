package com.rotation.controller.ui

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.rotation.controller.R

class PresetsActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.presets_activity)
        // Placeholder for presets functionality
        // In a full rewrite, this would be a Fragment or a Composable list of apps
    }
}
